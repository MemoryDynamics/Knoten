# Overview Paper

Dieses Verzeichnis enthält das zusätzliche Overview-Paper:

- `main.tex` — Working Preprint mit minimalem Modell und grundlegender Motivation

## Einordnung

Dieses Dokument gehört zu den konzeptuellen Notizen und ist daher hier in `docs/overview/` besser aufgehoben als im standardisierten `paper/`-Ordner.

## Workflow

1. Bearbeite `main.tex`
2. Kompiliere mit `pdflatex main.tex`
3. Pflege die Arbeit als Referenz für Paper I/II und weiterführende Modelle
