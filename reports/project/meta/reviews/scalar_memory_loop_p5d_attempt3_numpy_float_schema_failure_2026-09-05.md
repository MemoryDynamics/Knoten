# P5-D attempt-3 NumPy-float schema failure

Date: 2026-09-05.

Scientific status: **`p5d-inconclusive`**.

Recovery status: **closed; attempt 3 consumed and no further target invocation
authorized**.

This is an incident and readiness-coverage review, not a P5-D result review.
The explicitly authorized attempt completed the registered in-memory panel,
response reduction and classification, then failed closed during the first
v2 schema validation. It produced no JSON result, Markdown report,
publication manifest, temporary output or printed decision. Consequently no
response, ledger, symmetry or interaction pass/fail is observable from this
attempt.

## 1. Frozen execution identity

The invocation ran once from clean, upstream-synchronized authorization
revision `1104ebfdfceae3d9e9882c794273894352d6b1fd`. The frozen implementation
revision was `7d7f8a9eecaf72909cc937446d77e7589b48e24a`, validated by
[GitHub Actions run 33918599007](https://github.com/MemoryDynamics/Knoten/actions/runs/33918599007).
The governance-only authorization revision itself passed
[GitHub Actions run 33919081463](https://github.com/MemoryDynamics/Knoten/actions/runs/33919081463)
before target invocation.

Authorization UUID
`c948bdf3-01f6-43b0-aef9-d4d1a4373be1` was consumed by the exclusive receipt

```text
reports/dynamics/rotation/scalar_memory_loop_p5d_mutual_center_attempt_3.json
```

whose SHA-256 is
`33b837bc617352b133dffd6a6045dd4ee6d46829eda7859744ab71b8731f6aa1`.
The receipt records creation at `2026-09-04T21:05:20.993401+00:00` and binds
the authorization revision and governance digest. The runner was collected
after failure on 2026-09-05 Europe/Berlin. It emitted no progress or
scientific field before the traceback.

Before invocation, all receipt, result, report, manifest and audit paths were
absent. After failure, only the receipt exists. The three registered final
outputs, their three `.tmp` counterparts and the independent-audit output are
absent.

## 2. Exact failure boundary

The traceback reached `run_gate` after `_run_registered_panel`,
`panel_registration`, `response_controls` and `classify_panel`. Payload
serialization called the strict v2 validator, which stopped at:

```text
$.panel.channel_off_arms[0].ledger_rival_fractions.flipped_force_a:
expected finite number, observed float64
```

The observed value was a finite `numpy.float64`, not a non-finite numerical
trajectory value. The contract deliberately rejects unsupported NumPy scalar
objects rather than silently converting them. Thus the validator behaved as
specified; the production payload violated its registered Python-type
boundary.

## 3. Outcome-independent guaranteed source

Static control-flow inspection identifies the source without using or
reconstructing the lost scientific result:

1. every channel-off arm has `coupling == 0`;
2. `_run_arm` computes `energy_scale` as the maximum of the zero coupling
   energy and `numpy.finfo(float).tiny`;
3. for channel-off arms the latter wins and is a `numpy.float64`;
4. all six `rival_maxima` values remain native Python `0.0` because no active
   ledger is evaluated;
5. `rival_fractions` divides each Python zero by the NumPy energy scale,
   yielding six finite `numpy.float64` zeros;
6. the exact v2 schema permits finite native numbers but rejects NumPy
   objects, so every complete production payload fails before JSON encoding.

The first reported key reflects traversal order; the defect is not confined
to `flipped_force_a`.

## 4. Falsified readiness claim

The target-free readiness review claimed that a complete synthetic 64+768
production-schema payload traversed validation, serialization, rendering,
publication and independent audit, and that NumPy scalars failed with exact
paths. Both statements were individually true for the fixtures but were not
tested together on output constructed by the real `_run_arm` data path.

The synthetic channel-off fixture inserted native Python `0.0` values and an
energy scale of `1.0`; it did not reproduce the NumPy scalar generated by the
production not-applicable scale floor. The third invocation therefore
falsifies `p5d-runner-ready-target-still-closed` as sufficient target-readiness
coverage. It does not falsify the mutual-center interaction hypothesis.

## 5. Scientific and governance consequence

All three P5-D invocations are infrastructure-inconclusive. Attempt 3 reached
an in-memory classification, but that decision was neither printed, observed
nor persisted and disappeared with the process. Runtime, panel completion and
the serialization path do not reveal its value. Re-running, reconstructing or
guessing it would violate the one-shot contract.

The attempt-3 receipt permanently consumes the authorization. No fourth
target invocation, production-payload reconstruction or serializer/type
patch is authorized by the existing protocol. Any resumption requires a new
prospective governance decision and a separately reviewed protocol before
code changes. At minimum, such a protocol would need a target-free test that
constructs the exact production channel-off record through the real arm data
path or an outcome-independent equivalent, while preserving the frozen
scientific panel and decision rule.

Until then P5-D remains closed with no interaction evidence. No claim of
spontaneous interaction, charge, force law, spin, momentum, inertia, mass,
toroidal topology, mediator or quantization follows.
