# P0 audit: S1 candidate and discovery freeze

Date: 2026-08-16.

Decision: **`P0-fail-no-eligible-candidate-record`**.

Consequences: D0, D1 and every candidate target simulation remain blocked.
This is a provenance failure before topology, not a negative topology result.
It applies only to the `s1-topology` claim scope. The distinct normalized
memory-center candidate is assessed by a separate mechanics P0 and cannot
serve as the missing S1 candidate.

## Scope and evidence

The audit started from clean revision
`9f7fb20115ac042a3cfd59c930654780bb934bef` and searched:

- all local and remote-tracking branches, the active reflog and project
  history through 2026-08-16;
- candidate-, phase-, oscillation- and complex-mode reports and scripts;
- JSON/YAML metadata below `data/processed`, `reports`, `docs` and
  `experiments/current`;
- the exact P0 discovery, selection, confirmatory-seed and holdout field names
  registered in the
  [falsification charter](../../decisions/s1_phase_mass_falsification_program_2026-08-16.md).

No distinct new candidate manifest or discovery record was found. In
particular, `data/processed` contains no candidate-, phase-, oscillation- or
mode-named JSON manifest, and the registered discovery/selection fields occur
only as pending fields in the charter. Branches `master` and `cleanup` predate
the present oscillator program; `main` contains no intervening candidate
commit between the center-mass audit and the charter.

## Candidate inventory and quarantine

| record | useful evidence | reason it cannot pass P0 |
|---|---|---|
| [weak-self local witness](../../../response/reciprocal/reciprocal_local_mode_gate_2026-08-04.md), `lambda=0.01`, `g=0`, `c=0.02` | analytic stable complex relative pair | constructed local witness, not a formed full-knot trajectory, stationary recurrent set or discovered interacting candidate |
| [same-law common-scale midpoint](../../../response/reciprocal/same_law_common_scale_followup_2026-08-11.md), including `eta=0.0016265818` at `R=sigma_rep` | 13/13 local complex-matrix eligibility | unchanged [affine-balance gate](../../../response/reciprocal/same_law_affine_balance_gate_2026-08-11.md) is 0/13 with finite drift; no stationary nonlinear mode exists at that expansion point |
| [corrected scalar candidates](../../../kernels/mode_probes/ar_mode_probe_corrected_candidates_2026-07-09.md) `A_att={0.35,9,35}` | compact/relaxing scalar controls | all registered slow classifications are real; small complex residues are fast and lag-dependent |
| [vector-memory pilot](../../../vector_memory/vector_memory_pilot_initial_2026-07-10.md) `A_att={0.35,7.75,8,9,20}`, `eta_v={0,0.01,0.03}` | projected complex AR pairs | the same classifications occur in `eta_v=0` and [full scalar-off controls](../../../vector_memory/vector_memory_eta_s_zero_control_2026-07-10.md); no mechanism-separated phase candidate or complete discovery ledger |
| [reciprocal/Hankel pole candidates](../../../response/reciprocal/hankel_pole_identity_audit_2026-08-06.md) | recurrent fitted complex poles in selected readouts | identity audits find comparable poles in one-way/control arms; projection and segment selection are discovery-contaminated |
| [P3.8d witness](../../../response/reciprocal/dynamic_two_knot_mediator_gate_2026-08-12.md) `(delta,mu,r_gamma)=(-1.9,0.3,1)` | energy-consistent second-order mediator and a selected-mode period `17.907628` | force reversals vanish under equilibrium initialization; ringing is a damped quench transient and the Lyapunov balance excludes an autonomous limit cycle |
| [center-port continuum slice](../../../dynamics/limits/scalar_memory_center_inertial_port_gate_2026-08-16.md) | passive positive inertial realization of the memory center | center poles are zero and negative real; it supplies no internal oscillator and its port experiment did not discover one |

The [P3.8e canonical closure](../../../memory/closure/emergent_modal_state_reconciliation_2026-08-13.md)
also cannot supply the missing candidate. Its
corrected result is measurement-limited with 0/5 informative memory-plus-
visible holdout channels, 0/5 diagnostic second-state channels and 0/5
oscillatory channels. It is therefore an earlier D1-like inadequacy result,
not an S1 discovery source.

## Executable P0 result

The
[draft manifest](s1_candidate_p0_manifest_2026-08-16.json) records only facts
established by the audit and leaves candidate-specific values null. The
[machine audit](s1_candidate_p0_audit_2026-08-16.json), generated by the
[P0 validator](https://github.com/MemoryDynamics/Knoten/blob/main/experiments/current/topology/s1_p0_manifest_gate.py),
returns 27 independent completeness defects:

| block | missing or failing items |
|---|---:|
| candidate identity, claim, architecture, time law, revision, clean status and predecessor relation | 8 |
| complete noise/memory/kernel/coupling/integration/horizon/external/initialization tuple | 8 |
| initial-state source and hash records | 1 |
| discovery seeds, cadence, forcing, inspected observables/cells, selection rule and artifact hashes | 7 |
| confirmatory seed rule, untouched parameter holdout and explicit seed disjointness | 3 |

`target_data_opened=false` is the one confirmatory seal already satisfied.
The claim-scoped branch contract is also valid: it requests only D0 and seals
the mechanics gates, so it adds no defect and cannot be opened by a center
result.
The validator accumulates all defects instead of stopping at the first one,
checks full commits plus SHA-256 formats and contents of repository-relative
artifacts, canonicalizes text line endings before hashing, rejects dirty
discovery trees and requires a distinct untouched validation design.

Reproduction:

    python experiments/current/topology/s1_p0_manifest_gate.py

Exit code 1 is the registered result for the present incomplete manifest.

Software verification:

- focused P0, topology-control and experiment-catalog tests: 26 passed;
- complete repository suite with an isolated test temp directory: 650 passed;
- Ruff lint and format checks on changed Python files: passed;
- strict MkDocs build: passed.

## Evidence, inference and claim boundary

- **Evidence:** no eligible manifest/provenance record exists in the audited
  repository; every located oscillator-like record is negative, conditional,
  constructed or methodologically ineligible for this new claim.
- **Inference:** the possible parameter set mentioned in discussion is either
  not persisted in the repository or is not uniquely identifiable from the
  stored records.
- **Not inferred:** no old tuple is promoted, no parameters are reconstructed
  from a plot, and absence of a P0 record is not evidence that the model lacks
  an S1-like regime.

## Exact unblock condition

P0 may be rerun only after the draft manifest is replaced by one concrete
candidate record containing the full tuple and all discovery artifacts. If
the candidate came from an uncommitted interactive run, that run's exact code,
configuration, seed/state source, inspected alternatives and raw outputs must
first be reconstructed and hash-committed as discovery material. It cannot be
counted as confirmatory evidence.

Only a zero-issue P0 audit changes D0 to `authorized`. D0 must then freeze the
canonical raw observable, metric, symmetry actions and claimed topological
object before any target trace is inspected. D1 opens only after that D0
contract passes. The untouched synthetic method-validation split also remains
unopened.
